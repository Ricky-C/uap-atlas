// Claude multimodal pass: neutral summary, objectClass, redactionPct. Cached by
// source-file hash so a document is never re-billed. Arrives in Phase 2.
export function enrichRecords(): never {
  throw new Error("ingest/enrich: not implemented (Phase 2)");
}
